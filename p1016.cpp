#include <iostream>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	long long int minn, maxx;
	cin >> minn>>maxx;

	for(long long int i = minn; i)

	return 0;
}