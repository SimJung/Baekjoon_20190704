#include <iostream>
#include <string>
#include <vector>

using namespace std;

int solution(int n, vector<int> weak, vector<int> dist) {
	int answer = 0;
	return answer;
}

int main(void)
{
	vector<int> weak;
	vector<int> dist;

	weak.push_back(1);
	w
	return 0;
}