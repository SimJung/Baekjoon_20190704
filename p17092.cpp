#include <iostream>
#include <queue>
#include <vector>
using namespace std;

long long mo[10];
vector<pair<int, int>> visit;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int h, w, n;
	cin >> h >> w >> n;


	return 0;
}